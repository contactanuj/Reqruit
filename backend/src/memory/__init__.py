# Per-agent memory retrieval: hybrid search, recency queries, and summarization.
# Each agent gets a tailored memory recipe that blends semantic relevance
# (Weaviate) with temporal recency (MongoDB) for optimal context retrieval.
