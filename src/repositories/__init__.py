# Data access layer (Repository Pattern).
# Repositories encapsulate all database queries. Services never query the DB directly.
# This separation makes business logic easy to test with mock repositories.
